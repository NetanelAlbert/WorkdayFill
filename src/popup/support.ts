/** Bonusly slash command coworkers paste in Slack. Slack cannot prefill the composer via URL. */
export const BONUSLY_GIVE_COMMAND =
  "/give +100 @nalbert Thanks for the Workday extension #own-it";

/** Official Slack desktop deep link — opens the native app, not the website. */
export const SLACK_URL = "slack://open";

/** Public Buy Me a Coffee page. Leave empty until the page exists so the button is hidden. */
export const BUY_ME_A_COFFEE_URL = "";
